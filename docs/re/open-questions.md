# Open Questions

Track unresolved work here so future Codex sessions can pick it up quickly.

## Post-Login Loading Crash

- status: open
- area: runtime | protocol
- current belief: the original "login succeeds, then loading/update gets stuck or loops" blocker is now largely resolved. The remaining work has moved later into scene/resource flow after loading teardown, and appended logs need per-run markers to avoid mixing old and new evidence.
- confirmed update:
  - a prior emulator hook that directly rewrote scene tile table entries to backfill tiles `25/26`, plus a paired "skip missing tile draw" return hook, has now been removed from `src/runtime/emulator_runtime.c` because it was mutating in-game state to push progress.
  - because of that, older evidence that depended on reaching `scene_draw_status_panels` with valid tile `25/26` must now be treated as historical evidence from a modified runtime, not as proof of the unmodified path.
- confirmed evidence:
  - the newest static/runtime pass separates two different state families that had been conflated in the logs. The `runtime_state loadFlags=%u,%u,%u` tracer reads `R9+23673..23675`, but the early-return gate in `scene_runtime_tick()` checks `R9+0x5C64+3/+4` (`R9+23655/+23656`). These are not the same bytes.
  - `scene_runtime_tick()` now gives a concrete local readiness gate for the persistent loading overlay. Its first branch sequence is: if `R9+0x5C64+3 == 0` or `R9+0x5C64+4 == 0`, it calls `sub_1003568()` at `0x01014D80`, immediately calls `sub_1013BDC()`, and returns to the early scene-tick path instead of continuing into normal world/UI update. This means the continued `"正在加载中..."` overlay is currently explained by a local two-byte scene-tick gate, not directly by the previously logged `loadFlags=1,0,0`.
  - the overlay source chain is now consistent with that gate: `sub_1003568()` is a thin wrapper over `sub_100337C(0)`, and `sub_100337C(0)` draws the centered `正在加载中...` text. `scene_runtime_tick()` also has a direct code xref to `sub_1003568()` at `0x01014D80`, so the repeated later overlay draws do not require `scene_runtime_init_and_sync()` itself to keep looping.
  - the newest watcher run confirms those two scene-tick gate bytes are actually raised during fresh-enter initialization before the first `event=7` payloads are dispatched. `trace_scene_tick_gate_change` shows `0x010132C8/0x010132CA` changing the pair from `0,0 -> 1,0 -> 1,1` at `tick=231`, still inside `scene_runtime_init_and_sync()`.
  - the same run also shows that all later `trace_loading_overlay_call` lines on the scene screen occur while `sceneTickGate=1,1` and come from one-shot init/rebuild callsites (`0x01013058`, `0x010131B2`, `0x010131C0`, `0x0100F822`, `0x0100F898`, `0x01013598`, `0x01013666`, `0x010136B8`, `0x01013772`, `0x010137AC`, `0x010137BA`, `0x010137E0`, `0x010137F2`), not from a later repeated `scene_runtime_tick()` early-return loop.
  - once the first `event=7` business packet lands at `tick=232`, the client consumes `0x1E/1`, `0x1E/3`, and `0x1E/7`, follow-up scanning resumes, and there are no newer `trace_loading_overlay_call` entries in the same session tail. This makes the older “persistent loading overlay is the active blocker” hypothesis substantially weaker.
  - a related conditional path is now clearer in static code: later in `scene_runtime_init_and_sync()`, if `*(R9+0x54AC + 0x3D0) == 1`, the function explicitly clears `R9+0x5C64+4` again at `0x0101365E` and sends `scene_send_type27_followup_request(1)`. The newest watcher run did not observe this clear, so that branch was not taken in the current scene-enter path.
  - the other repeating text helper in the same logs is now identified as a different screen family, not the scene loading overlay. `lcd_text_call guestCaller=01034bc7/01034c15` draws the multiline text `正在和服务器通讯请稍候....`; IDA shows `0x01034BC7` and `0x01034C15` are inside `sub_1034AC8`, a generic multiline text renderer, so those lines should not be confused with the `sub_100337C(0)` / `正在加载中...` scene-overlay path.
  - in the latest rerun, `runtime_state label=post_data_event dispatch=01012e4d ... sceneGate=0 sceneState=7 activeScreen=01053450` is reached at `tick=138` after `0x1E/1`, `0x1E/3`, `0x1E/7`, and follow-up scanning. No newer `trace_loading_overlay_call` lines appear after that first fresh-enter `event=7` cluster in the same session tail, which is the strongest current evidence that the original post-login loading blocker is no longer active in that later phase.
  - the newest clean session also shows the client continuing with local scene/resource work after that first fresh-enter callback cluster rather than immediately faulting or re-requesting network state. `logs/storage_trace.log` records successful reads of `c00蓬莱仙岛_01.sce`, `00蓬莱仙岛_01.map`, multiple actor resources (`b_bamboo.actor`, `b_flowers01.actor`, `ui_h_war.actor`, etc.), and long `item.dsh` scans, with no new `file_open_fail` beyond the old early-boot optional files (`mmorpgTempdata`, `Wpay9990Ker42WqvgaV100.CBM`).
  - the matching `logs/net_trace.log` tail shows no later `scene_send_map_enter_request`, no later `type=6` update/download cycle, and no later `host_signal` after `tick=138`; the remaining gap therefore looks more like an in-scene local mode/content state than a repeated network or resource-loader failure.
  - the newest stdout-only crash evidence adds a different local failure mode after `ScreenInit Ok`: `地址无法访问:4 type:0 size:19 value:2` with `pc=lastPc=0x0100E67C` and `lr=0x0100E8E3`. IDA maps this to `draw_image_tiled_clipped()` dereferencing `LDRH R1, [R0,#4]` with a null image pointer, called from `draw_map_tile_by_index()`.
  - static xrefs for `draw_map_tile_by_index()` show likely live callers on the scene screen include `scene_draw_status_panels()` (`0x010146A4` / `0x010146B8` for tiles `26` and `65`) and several `scene_runtime_tick()` draw sites. This makes the current crash look like a null tile/image resource entry in the local map/tile table, not a renewed network-stall or update-loop.
  - the newest rerun with `trace_draw_map_tile_entry` and `stdout_trace` confirms the first null entry is exactly the status-panel background path. After fresh-enter completes (`sceneGate=0`, `sceneState=7`), `scene_runtime_tick()` reaches normal draw pass (`0x01015158`) and then `scene_draw_status_panels()` (`0x0101517A`). The very next traced map draw is `tileId=26 x=0 y=0 w=240 h=32 repeat=3`, with `tileIndex=-1` and `resPtr=0`, called from `0x010146A4`. This matches the immediate stdout crash at `pc=0x0100E67C`, `lr=0x0100E8E3`.
  - local assets and storage traces further narrow the contract gap: the current `.sce -> .map` path resolves map art such as `m_peak1.gif`, `m_village05.gif`, and `m_apotheosis.gif`, and the repository also contains `JHOnlineData/UI2.gif`. The failing `tileId=26` therefore looks more like a missing default/UI tile-table seed than a missing scene-map art file.
  - static piclib analysis now explains the watcher PCs more concretely. `sub_10439E4()` (`scene_system_bootstrap()` calls it with `n96=96`) allocates the scene piclib and initializes every `tileInfo[i].tileIndex` to `0xFFFF` in the loop that includes `0x01043BA6`. Later, `sub_10433BA()` is the sole per-tile load path, and its success path runs through `0x01043420`, where it writes the resolved resource slot back into `tileInfo[a2].tileIndex`.
  - the same static pass confirms `tileId=26` is not a scene-map art slot: `sub_10434CE()` seeds the fixed piclib name table and entry `26` is `UI2.gif`. A newer decompilation corrects an older note here: the same fixed table actually seeds names through `tileId=94`, including `tileId=65 -> UIsh7.gif`, while `scene_system_bootstrap()` still allocates 96 slots total.
  - the newest runtime trace makes the asymmetry explicit. In the latest clean session there are `trace_piclib_slot_load` lines for `tileId=65` only: `sub_10433BA()` is entered with `name=UIsh7.gif`, and the success path at `0x01043420` writes back `tileIndex=27`, after which `trace_status_tile_slots` reports `tile65=27 res65=050248f0`. There are no matching `trace_piclib_slot_load` lines for `tileId=26` anywhere before the crash, even though the later draw still calls `draw_map_tile_by_index(26, ...)`.
  - static code identifies the active requester for those successful later fills. `scene_runtime_init_and_sync()` calls `sub_1045EA4()` at `0x010137B6`, and `sub_1045EA4()` is a hard-coded scene tile preloader that repeatedly invokes the piclib `loadTile` method for many ids (`24, 40, 39, 41, 27, ... 59..65, 46..49, 0..7, 70, 71, ... 77`) but never requests tile `25` or `26`. This matches the live trace where `65` is requested and `26` is never seen.
  - the widened runtime snapshot now refines that family-level guess into a smaller persistent gap with a concrete earlier caller class. In the newest clean run, `trace_piclib_slot_load` shows `tile31 -> UI8.gif` and `tile30 -> UI7.gif` being requested and loaded first at `tick=78` from guest LR `0x05019139/0x05019141` on `activeScreen=010534b4`, well before the later scene preloader runs. Then `sub_1045EA4()` later loads `tile27/28`, and `tile65` loads as `UIsh7.gif`; only `tile25`, `tile26`, and `tile29` stay at `tileIndex=-1` through the first in-scene draw pass. So the live issue is no longer “none of the `UI1..UI8` family is seeded”, but “an earlier path partially seeds that family and consistently skips `25/26/29`”.
  - a new static pass on `mmTitleMstarWqvga.cbm` now ties that earlier `activeScreen=010534b4` phase to a concrete title-module transition screen. `sub_C82()` builds a screen object via `sub_C54() -> sub_2CA()`, and that screen's init callback `sub_A50()` explicitly requests `UI2.gif`, `UI8.gif`, and `UI7.gif` by name. The same session's storage trace shows `UI8.gif` and `UI7.gif` reads before `mmGameMstarWqvga.cbm` is opened, which strongly suggests the first `UI7/UI8` activity belongs to a post-login title-module screen, not to the later scene/game module itself.
  - that same title-module finding also narrows the remaining contract gap: the title screen's `sub_A50()` uses module-local named-image loads, while the later crash is in the scene piclib slot table (`sub_10433BA()` writing `tileInfo[tileId].tileIndex`). So even if `UI2.gif` is touched or cached earlier at the title-module layer, that does **not** prove scene tile `26` was seeded. The strongest current reading is that `tile30/31` may be benefiting from an earlier cross-layer resource/cache path, while `tile26` still lacks a scene-piclib-native request.
  - the next static pass clarifies that “cross-layer cache path” wording. There is now positive evidence for a shared lower-level resource/file-open layer, but still no evidence for a shared scene-slot layer. The scene piclib loader `sub_1043206()` opens `\\JHOnlineData\\%s` through `sub_100D2BE()`, and the earlier title/startup-style named-image opens for `UI8.gif` / `UI7.gif` hit the same helper path in storage trace. But the higher-level owners keep their state separate: title-module `sub_A50()` stores returned handles in local fields `R9+10348/10352/10356`, main-CBE startup/update helper `sub_103B0B0()` stores `UI2/UI8/UI7` into its own startup object fields `+52/+54/+56`, and scene piclib still tracks its own `loaded/tileIndex/w/h` table at `R9+0x9AF0`. No static copy/bridge into scene `tileInfo[26]` has been found.
  - the same pass also sharpens the second branch of the question. On the scene side, the only confirmed explicit preloader before the crash is still `sub_1045EA4()`, and it hardcodes many tile ids but continues to skip `25/26/29`. `scene_draw_status_panels()` itself only consumes tile `26` / `65` through `draw_map_tile_by_index()` and does not lazy-load. So if there is no hidden slot bridge, the missing contract is now best described as “some separate scene-local status/UI init path was supposed to call `loadTile(26)` before `R9+0x5C64+0x15` made status-panel drawing eligible, but that path has not yet been identified”.
  - the newest scene-local pass strengthens that wording in two ways. First, the normal `scene_runtime_tick()` draw order is now pinned: `scene_sort_actor_draw_order() -> scene_draw_softkey_overlay() -> scene_draw_status_panels() -> scene_draw_actor_pass() -> ...`, so the first crashing `tile26` draw happens before later prompt/input helpers can affect the same frame. Second, a focused scan for functions that both touch `R9+0x9AF0` and actually call the piclib `+0xC` method found one real scene-side caller, `sub_10461D0() -> (*piclib->loadTile)(0x13)`, and that caller is for tile `19`, not tile `26`.
  - the apparent second `+0xC` hit from the same scan (`sub_1024448()`) is not evidence of another scene tile-preload path: the `+0xC` callback in that instruction window belongs to a different object, not the piclib object loaded from `R9+0x9AF0`. So the current “no confirmed `loadTile(26)` caller before first status draw” conclusion still stands after a broader mechanical sweep.
  - the newest consumer-side split weakens the old “maybe `UI2` belongs to the same initializer as `UI3/UI4/UI7/UI8/UIsh7`” idea. The actual scene consumers divide into separate families: `scene_draw_status_panels()` uses `26`, `65`, and piclib tiles `59..62/64`; `sub_1013A7A()` / `sub_100E8E4()` use `30/31`; and popup/list helpers `sub_10489E8()/sub_1048B14()` and `sub_101A13A()` use `27/28`. This makes the active gap status-panel-specific rather than “one missing member of a broad shared UI family”.
  - that split also suggests a more precise contract failure: `sub_1045EA4()` already looks like the status-panel strip/icon preloader because it requests `59..65`, which are exactly the later scene-local status-panel tiles. The one piece still outside that contract is the full-width background `tile26/UI2.gif`. So the best remaining candidates are now:
    - a dedicated `loadTile(26)` that should have lived next to the `59..65` status-panel preload sequence but is missing/never reached
    - a one-time bridge from the earlier startup/title named-image `UI2.gif` owner into scene tile `26`, while the rest of the scene-local status panel uses explicit `59..65` piclib loads
  - the newest name-table pass makes that even less speculative. `sub_10434CE()` maps `tile59..65` to `UIsh1.gif` .. `UIsh7.gif`, and `sub_1045EA4()` preloads that exact contiguous `59..65` run. `scene_draw_status_panels()` then consumes the same status-panel cluster in one function: `tile26` background first, `tile65` second, then piclib tiles `59..62` and `64`. So the current asymmetry is no longer just “one missing UI tile near some others”; it is “the status-panel strip half of the contract is explicitly preloaded, but the status-panel background half (`UI2.gif`) is not”.
  - the latest near-neighbor pass also weakens the remaining “maybe there is a sibling/alternate preloader right next to `sub_1045EA4()`” idea. In the current main CBE, `sub_1045EA4()` still has only one direct caller (`scene_runtime_init_and_sync()`), and the surrounding `0x1045Bxx..0x1045Exx` helpers are generic `.dsh`/data-sheet loader object methods initialized by `sub_1045E0A()`, not second status-panel preload branches. No second same-level repeated-`loadTile` helper touching `26` or `59..65` was found in the local range.
  - the same static pass now narrows the immediate neighborhood around that preloader too. `scene_system_bootstrap()` only constructs the scene piclib object via `sub_10439E4()` and seeds the fixed filename table; it does not request any individual tile ids before returning. Later, inside `scene_runtime_init_and_sync()`, the status-panel dirty/visible byte at `R9+0x5C64+0x15` is set to `1` at `0x01013780` before `sub_1045EA4()` is called, which means the first `scene_draw_status_panels()` pass becomes eligible before any explicit local request for `tile26` has been observed.
  - after `sub_1045EA4()`, the only remaining local branches in `scene_runtime_init_and_sync()` are deferred popup/controller checks around `R9+0x5EF0` (`0x010137C6`, `0x01013808`, `0x01013836`, `0x01013864`). The helper object initialized by `sub_1048FEE()` is just a callback-forwarder (`sub_1048F7A/sub_1048F8C/sub_1048FCA/...`) and does not itself touch the scene piclib or request tile ids. That makes the old “maybe `sub_1045EA4()` has a nearby alternate preload branch for `25/26`” hypothesis substantially weaker.
  - the status-panel flag bytes are also clearer now. Static writes show `scene_runtime_init_and_sync()` sets `R9+0x5C64+0x15 = 1` and clears `+0x16/+0x17` at `0x01013780..0x01013786`, while `scene_draw_status_panels()` later clears only `+0x16/+0x17` at `0x010147C0/0x010147C2`. Other scene-local writers (`sub_100ECDE()`, `scene_handle_hover_prompt_input()`, `net_handle_group_info()`, `sub_10159DA()`, `scene_draw_softkey_overlay()`, and actor-move parser `sub_100F094()`) toggle those same bytes but do not call the piclib loader in their nearby static code. The strongest current reading is therefore that `+0x15` is a persistent visible/enable bit, `+0x16/+0x17` are redraw/dirty bits, and the current live path is enabling status-panel drawing before the missing UI-family seed has happened.
  - the newest clean session changes the branch priority again: after login `actorinfo`, the client does **not** emit `trace_scene_send_map_enter_request`, `kind=30 subtype=2`, `kind=30 subtype=3`, or any `27/4` follow-up. The fresh-enter path now stops after the built-in `type=1/2/3` triplet, with only `trace_business_handler pc=01039b8a kind=30 subtype=1` observed at `tick=119`.
  - the same session shows the `"正在加载中..."` overlay helper is not being re-entered indefinitely after scene-enter packets land. All traced `sub_1003568() -> sub_100337C(0)` calls on the scene screen happen during `tick=118`, before the queued `event=7` deliveries at `tick=119`, and all traced callers are inside `scene_runtime_init_and_sync()` (`0x01013058 .. 0x010137F2`) or `scene_rebuild_runtime_nodes()` (`0x0100F822`, `0x0100F898`).
  - after those `tick=118` local overlay draws finish, the log still ends with `sceneGate=0`, `sceneState=7`, `loadFlags=1,0,0`, but there are no later `lcd_text_call ... text=正在加载中...` lines in the same session tail. This weakens the earlier idea that a later draw loop is continuously reasserting the loading text after the first scene-enter callback.
  - static scene-channel dispatch keeps `0x1E/3` in scope for this fresh-enter stall. `net_handle_scene_channel_dispatch()` routes subtype `1` to `parse_scene_response_entry(scene,posinfo)`, subtype `2` to `parse_scene_posinfo_field(result,scene,posinfo)`, and subtype `3` to `parse_scene_npcinfo_list(curpage,pagenum,roomnum,roomlist,...)`. The current fresh-enter run only hits subtype `1`.
  - the latest rerun confirms the new `0x1E/3` experiment is consumed but still not sufficient. `mock_group_type1_response ... sceneRoomNpc=1 len=259` is followed by `trace_business_handler pc=01039b8a kind=30 subtype=3` and `trace_business_handler pc=01039222 kind=30 subtype=3`, yet the callback still ends with `sceneGate=0 sceneState=7 loadFlags=1,0,0`.
  - this pushes the question one layer deeper: the problem is no longer “missing `0x1E/3` entirely”, but “the minimal `0x1E/3` field set is semantically too weak”. Static code also suggests `0x1E/3` is room-list specific: `parse_scene_npcinfo_list()` consumes `curpage`, `pagenum`, `roomnum`, and `roomlist`, while a different scene-channel handler `sub_1039430()` (dispatch case `7`) consumes `roomid`, `rolenum`, and `rolesinfo`.
  - the newest rerun confirms the same pattern for `0x1E/7`: `mock_group_type1_response ... sceneRoomRoles=1 len=307` is followed by `trace_business_handler pc=01039b8a kind=30 subtype=7` and `trace_business_handler pc=01039430 kind=30 subtype=7`, yet the callback still ends with `sceneGate=0 sceneState=7 loadFlags=1,0,0`.
  - this means the current blocker is no longer “which scene-channel subtype is missing” for the fresh-enter packet. Subtypes `1`, `3`, and `7` all land on their consumers in the same first `event=7`, but the all-empty `roomlist` / `rolesinfo` payloads still do not satisfy whatever scene-content readiness condition eventually clears the loading state.
  - a newer static pass narrows the remaining omission further: both `parse_scene_npcinfo_list()` (`0x01039222`) and `sub_1039430()` (`0x01039430`) begin by calling `scene_parse_column_names()` (`0x01039180`), which explicitly reads packet fields `colnum` and `colnames` before iterating `roomlist` / `rolesinfo`. The previous built-in `0x1E/3` / `0x1E/7` mocks did not provide either field, so the client was consuming row blobs without the list-header metadata these handlers are prepared to parse.
  - the same decompilation also refines the current row-shape understanding: `0x1E/3 roomlist` is one `u32` plus three strings per room entry, while `0x1E/7 rolesinfo` is four strings per role entry. The current one-record mock already matches that row shape, so `colnum/colnames` are now the clearest next protocol-semantic candidate before we guess at deeper row contents again.
  - the newest rerun shows that simply appending guessed `colnum/colnames` is not yet correct. The packet grows as expected (`mock_group_type1_response ... len=477`), and `kind=30 subtype=3` still enters `parse_scene_npcinfo_list()` at `tick=250`, but the log now stops at `trace_business_handler pc=01039222 ... subtype=3` before any later `kind=30 subtype=7`, `trace_followup_scan_enter`, or `post_data_event` line appears. That strongly suggests the current `colnum/colnames` encoding or semantics are wrong enough to stall inside the `0x1E/3` path itself.
  - the new list-header trace now points to a specific encoding mistake rather than a generic semantic gap. In the latest run, `scene_parse_column_names()` logs `trace_scene_colnames_item index=0 len=28`, which exactly matches the whole `colnames` blob size, not the first column name. The next logged item then misaligns into packet field text (`text=roomnum`). This strongly suggests the current mock is wrapping `colnames` (and likely the parallel `roomlist` / `rolesinfo`) with an extra inner `u16 dataLen` layer that these scene-list consumers do not expect.
  - on the latest post-revert run, the current log no longer ends in a crash after login. The client reaches the loading screen (`activeScreen=01053f78`, `currentThis=01053f60`) and consumes the mocked login reply plus three follow-up game replies, but the scene remains stuck with `loadFlags=1,0,0` and no later `host_signal` appears in the newest tail.
  - the latest connect-2 sequence is: login request (`len=86`) -> built-in login reply (`len=443`) -> post-login request `type=1` (`len=35`) -> built-in group/type1 reply (`len=129`) -> `type=2` (`len=19`) -> built-in reply (`len=34`) -> `type=3` (`len=19`) -> built-in reply (`len=36`).
  - in static code, `scene_runtime_init_and_sync()` (`0x01012FB4`) installs `net_business_response_dispatch`, consumes cached scene-entry fields, and then queues the first post-scene business sync request. The same function comments and strings show scene-entry state depends on `revivetype`, `ruffianflag`, `type`, `pcimg`, `expcard`, `expbook`, `practiseinfo`, `lastexp`, and related follow-up sync data.
  - `net_business_response_dispatch()` only routes real scene-position updates through `net_handle_scene_posinfo_response()` (business kind `0x1C`), and that handler explicitly expects fields `scene` and `posinfo`.
  - the latest rerun disproves the older protocol note that simply switching `type=2/3` replies to `kind=0x0A subtype=0x1A` with `scene+posinfo` is enough. The new payloads are delivered (`len=75` each), but `loadFlags` remain `1,0,0` and the loading screen still does not progress.
  - static code explains why that retry was ineffective: business kind `0x0A` dispatches to `net_handle_role_login_gift_glamour()`, whose known subtype handlers operate on fields like `name`, `money`, `giftinfo`, and `glamour`, not `scene`/`posinfo`. Real scene-position updates instead flow through business kind `0x1C` or scene-channel kind `0x1E`.
  - the same trace also strengthens the opposite side of the mapping: `scene_runtime_init_and_sync()` explicitly depends on `pcimg` and `expcard`, and `net_handle_misc_player_fields()` is the confirmed handler that stores those two fields from business kind `7` subtype `20/32`.
  - the newest rerun after reverting `type=2/3` confirms those packets now hit `net_handle_misc_player_fields()` exactly as expected: `kind=7 subtype=20` and `kind=7 subtype=32` are both observed at `tick=129`, but `sceneGate` still remains `1` and `loadFlags` still remain `1,0,0`.
  - IDA decompilation now gives a concrete scene-enter candidate: `parse_scene_response_entry()` (`0x010396D6`), dispatched through scene-channel `kind=0x1E subtype=1`, consumes `scene + posinfo`, sets `sceneState=7`, and clears `sceneGate`. The current post-login flow never reaches that handler.
  - the latest scene-enter experiment is confirmed successful: once `kind=0x1E subtype=1 scene+posinfo` is appended to the post-login `type=1` reply, the client does hit `trace_business_handler pc=01039b8a kind=30 subtype=1`, `sceneGate` clears to `0`, `sceneState` becomes `7`, and the client emits a real `scene_send_map_enter_request`.
  - after that transition, the client sends a new composite `len=86` request that contains more than just map-enter fields. Decoding it with the existing WT request-object helper shows object sequence `1/0x0C/1`, `1/7/42`, `1/2/3(maptype,mapID,exitID)`, `1/0x1B/11`, `1/0x0C/1`, `1/7/42`.
  - the request/response pairing is now narrower: the current built-in `0x1B/12` is only appended when that `len=86` request contains `1/0x1B/11`, so the live `27/12` object is best understood as the server-side answer to the embedded `27/11` subrequest inside the scene-enter follow-up packet.
  - `scene_send_map_enter_request()` serializes request field `mapID` from `uiState + 1141`, and `ui_apply_named_posinfo_target()` writes incoming `0x1B/12.name` into that same `uiState + 1141` buffer. This is now `confirmed` by IDA decompilation (`0x0100F9B4`, `0x0100E9B8`) and strongly argues that the right `0x1B/12.name` should be map/scene/location text, not an actor/resource filename.
  - the shared-buffer producer/consumer chain is now narrower too. Around scene-enter, `uiState + 1141` has two explicit writers:
    - `ui_apply_named_posinfo_target()` from `27/12`
    - `scene_rebuild_runtime_nodes()` from its `currentMapIdText` argument, but only when `varg_r0_1 != 0`
    - `net_handle_actor_move_info()` case `12`, which also clears the slot and copies incoming business field `name`
  - `scene_runtime_init_and_sync()` uses those writers differently by branch:
    - fresh scene-enter path prepares a temporary text buffer at `R9+0x5E46`, validates it via `sub_100EEBC()/sub_100FA40()`, then calls `scene_rebuild_runtime_nodes(varg_r0_1=1, ..., currentMapIdText=R9+0x5E46)`, which overwrites `uiState+1141`
    - pending scene-switch / restore path first validates the existing `uiState+1141` with `sub_100FA40(uiState+1141)`, then calls `scene_rebuild_runtime_nodes(varg_r0_1=0, ...)`, which preserves the existing `uiState+1141` contents
  - this means restore/re-enter logic expects `uiState+1141` to remain a valid current scene/map text slot across transitions, which explains why a wrong synthetic `27/12.name` poisons both later `mapID` sends and the `scene_rebuild_runtime_nodes() -> sub_10352AE()` path.
  - the validator used at both fresh-enter and restore callsites strengthens the semantic guess further: `sub_100EEBC()` accepts the text when either a manager compare callback approves it or the first byte is `'c'`. Combined with the observed outgoing `mapID=c00PenglaiXiandao_01`, this makes a c-prefixed internal scene id more likely than a natural-language map display name.
  - a related fallback helper, `sub_100EEE0()`, does not write `uiState+1141`; it only checks whether the current slot is valid and, if not, caches caller-supplied display names into a separate 4-entry fallback table. Its observed callers are actor/scene-node parsing and NPC parsing paths. That makes actor/NPC display names look secondary to the main `+1141` scene-text slot.
  - the previous built-in scene-change response only answered the `maptype/mapID/exitID` part with `kind=0x1E subtype=2`, which is enough to reach `trace_business_handler pc=01039b8a kind=30 subtype=2`, but still leaves the client stuck on loading with `loadFlags=1,0,0`.
  - the latest rerun with the composite scene-change reply confirms the client consumes `kind=0x1E subtype=2` and then `kind=7 subtype=42`, starts drawing the scene screen (`activeScreen=01053450`), and still keeps `loadFlags=1,0,0` plus the `"正在加载中..."` overlay. No trace has yet shown that the appended `kind=0x0C subtype=1` or `kind=0x1B subtype=11` objects were consumed.
  - the newest rerun with follow-up tracepoints removes that uncertainty: after the composite scene-change reply, `net_handle_business_followup_events()` is definitely entered, and both appended follow-up objects are consumed there. The log shows `trace_followup_case label=case27_11 kind=27 subtype=11` and `trace_followup_case label=case12_1 kind=12 subtype=1`, yet `loadFlags` still remain `1,0,0`.
  - the newest handler trace also shows that the primary `kind=27` dispatcher is entered for subtype `11`, but static code says `net_handle_fb_target_dispatch()` only has real logic for subtype `3`, `6`, and `12`. That makes the current empty `27/11` reply increasingly suspect as an incomplete semantic stand-in rather than the true server-side response shape.
  - the latest `0x1B/12` experiment is confirmed useful: once the composite scene-change reply also appends a minimal `27/12`, the client immediately emits a new composite `len=34` request containing `0x19/5`, `0x1B/11`, `0x1B/4(type=1)`, and `0x07/42`. This is the first concrete post-enter request beyond the earlier map-enter bundle, and it strongly suggests the next missing semantic sits in the `27/4` follow-up exchange rather than in `0x1E/3` yet.
  - the latest rerun after adding a minimal `27/4` combo response does progress further, but it currently diverts into a resource-update branch instead of clearing the loading state. The client emits repeated `type=6` WT requests with `start` offsets for `MMORPGTempcbm`, and the emulator serves all chunks successfully.
  - storage and static evidence now identify a concrete runtime-contract bug in host file I/O rather than a missing scene packet: `sub_1037000` (`0x01037000`) opens `MMORPGTempbin`, appends each update chunk, and only enters its completion branch when `current_filesize + chunk_len == totalsize`. In the latest `logs/storage_trace.log`, the writes are `20480 + 20480 + 7898`, but host `file_getfilesize` reports `20567` then `41153`, so the final equality never holds and the code never reaches its built-in `close(handle) -> sub_10368CA("MMORPGTempbin", ...) -> sub_1036768()` install/cleanup path.
  - the size inflation matches a Windows text-mode host-open bug: `vm_cbfs_vm_file_open()` passed the client hint `w+` through unchanged for `JHOnlineData/MMORPGTempbin`, so `fopen()` translated `0x0A` bytes while writing binary update payloads. This explains why `MMORPGTempbin` never closes or renames even though all chunks arrive.
  - after fixing host binary-open semantics, that runtime bug is now confirmed resolved: `MMORPGTempbin` grows as exact byte counts (`20480 -> 40960 -> 48858`), then `file_close` and `file_rename ... -> MMORPGTempcbm` appear as expected.
  - a new protocol-semantic issue is now exposed immediately after that successful install step: the client repeatedly tries to open `JHOnlineData/Codex`, and the same `type=6 start=0..` update cycle restarts. Static code identifies the open site as `sub_100D2BE`, the generic `\\JHOnlineData\\%s` resource-loader helper, and runtime logs show the repeated update request payload itself also carries `name="Codex"`.
  - the newest rerun after changing the synthetic `0x1B/12 name` to `MMORPGTempcbm` confirms that specific loop is gone in the latest appended evidence. `logs/storage_trace.log` no longer ends with `file_open_fail path=JHOnlineData/Codex`; instead it shows a successful local open of `JHOnlineData/MMORPGTempcbm` (`mode=rb`, first bytes `fefefefe`) from the same `sub_100D2BE` loader path.
  - the same latest appended evidence also shows the post-update loading screen being torn down cleanly. At `tick=69`, queued event `7` into callback `0x0103489B` removes screen `0105a518`, resumes `01056204`, and leaves `runtime_state` at `sceneGate=0 sceneState=0 loadFlags=0,0,0`; the following `after_data_event` line reports `updateState=2`, `downloadState=0`, `hasLocalUpdate=1`, and flags `1,1,1,1`.
  - because both log files are append-only, older `Codex` update-loop lines and newer successful `MMORPGTempcbm` lines can coexist in one file. The emulator now writes a `==== session_start channel=... ====` marker to both `logs/net_trace.log` and `logs/storage_trace.log` on first use per process so future reproductions can be separated cleanly.
  - static IDA evidence now narrows the semantic mismatch more sharply: `0x1B/12` field `name` is consumed by `ui_apply_named_posinfo_target_with_fb() -> ui_apply_named_posinfo_target()`, which copies `name` into active UI state and applies it as a named target/location together with optional `posinfo`. That is a display/target semantic, not a resource-filename semantic.
  - the later `type=6` request uses a different semantic: `send_update_chunk_request()` serializes `name/screen/type/start/version` from a pending-download table. That table is filled by `sub_1044DA0()/sub_1044EF6()` only after `sub_100D2BE()` fails to open a local resource by name. So `type=6.name` is a local resource filename key.
  - IDA now narrows the immediate caller set for the resource-open helper: `sub_100D2BE()` has only four code xrefs in `江湖OL.CBE`, and only two of them are plausible post-login/resource-update bridges in the current path: `sub_1043206()` and `sub_1044DA0()`. The other two are the local wrapper `sub_100D338()` and `load_resource_stream_by_name()`.
  - IDA also pins down where the later `type=6` request gets its serialized name in the resource-download branch: in `send_update_chunk_request()` case `n2==4`, `name` comes from `*(R9+38284) + 2`, `type` from the first `u16` in that same pending entry, `start` from offset `+52`, and `version` from `R9+38328`.
  - the latest runtime trace narrows the live path further: after `0x1B/12` is consumed at `tick=133`, the active UI cached name becomes `MMORPGTempcbm`, and the first later open of that same string is `trace_resource_open_helper ... name=MMORPGTempcbm uiName=MMORPGTempcbm` with `lr=0100D49D`, which means the string reaches `sub_100D2BE()` through `load_resource_stream_by_name()`, not through `sub_1044DA0()/sub_1044EF6()` or a `type=6` pending-download request.
  - the same newest session shows no `trace_update_request_prepare` line for `MMORPGTempcbm`, which further supports that this specific reuse is a direct resource-stream load rather than a pending download/update serialization.
  - the newest narrower callsite trace identifies the immediate caller: the `MMORPGTempcbm` string is carried by `trace_resource_stream_call_from_actor_motion` at `0x0100D6FC`, then by `sub_100D534()` and `load_resource_stream_by_name()`, before reaching `sub_100D2BE()`. The parallel traces for `0x0100D570` and `0x0100DB9C` continue to carry normal actor/UI resource names on the same run.
  - the newest entry trace sharpens the data-flow source: at `parse_actor_motion_descriptor()` entry (`0x0100D6E2`), the misrouted string is already present in the first stacked extra argument, while register `R0` continues to point at a non-string parser/object buffer. The caller `LR` is consistently `0x010352D7`, i.e. the indirect call inside `sub_10352AE()`.
  - the direct `sub_10352AE` callsite trace now confirms the upstream source: `scene_rebuild_runtime_nodes()` at `0x0100F8F6` is the first observed caller that passes `stack0Name=MMORPGTempcbm`, and that stacked argument then becomes `arg4Name=MMORPGTempcbm` at `parse_actor_motion_descriptor()` entry. This closes the active misroute from `0x1B/12.name` into the motion-resource loader.
  - based on that confirmed chain, continuing to use `MMORPGTempcbm` as the synthetic `0x1B/12.name` is now known-bad protocol emulation: it makes a scene-target/name field masquerade as a motion-resource filename.
  - `mock_missing_motion.actor` is also no longer an acceptable default fallback, because it is a hand-authored verification asset rather than a real game resource. The current minimal correction is therefore to fall back to the existing real actor resource selected by job/sex (`h_warrior.actor`, `hW_warrior.actor`, `h_assassin.actor`, `hW_assassin.actor`, `h_mage.actor`, `hW_mage.actor`) unless `CBE_ACTOR_MOTION_RESOURCE` is explicitly overridden.
  - the newest rerun confirms that plain real `.actor` resources are still the wrong format for this slot. With the fallback now set to `h_warrior.actor`, the client opens that file successfully, but the next open attempt becomes a malformed concatenated name (`iorwalk1.gif...h_warriorwalk2.gif...jian.gif`), which indicates `parse_actor_motion_descriptor()` is mis-parsing a normal actor/image-sequence file as a motion-descriptor file.
  - a repo-wide local asset scan supports that interpretation: tracked `bin/JHOnlineData/*.actor` files all currently have type byte `0x02` at offset `+4`, and none of them match the mock motion-wrapper marker `0xF1`. So the repository does not currently contain a confirmed real local motion-descriptor sample for this path.
  - after removing the progress-forcing scene hook and reproducing from a clean runtime, the first real blocker moved earlier than scene/status drawing: at `tick=1`, main CBE resource init calls `vmDfDataPackageManager` entry `idx=0` twice from the `0x0103B9FC` path, and the earlier stubbed behavior caused an immediate client-side abort.
  - wiring `vmDfDataPackageManager idx=0` to the existing `vm_DF_DataPackage_LoadPackage()` implementation removed that first blocker; the next exposed missing entry is `idx=4`, reported by the runtime as `[impl]vmDfDataPackageManager调用位置:4`.
  - `src/vmFunc.c` already contains a matching `VM_DF_DataPackage_DoLoading()` implementation for `idx=4`, and the runtime hook now routes `idx=4` to that function as the next minimal confirmed fix.
  - after wiring `vmDfDataPackageManager idx=0/4`, the client now gets far enough to display the loading/version UI and only aborts later at `tick=66`, right when queued network event `5` fires into callback `0x0103489B` (`net_wrapper_event_dispatch`).
  - the final `lastAddress` before that abort is `0x01034524`, which is inside `sub_1034518`, a packet-field/event-packet initializer used by `event_packet_add_field` and `event_packet_parse_WT`, not a scene/status draw site.
  - `vm_initMemoryBlock()` in `src/vmFunc.c` documents that `VM_MF_MemoryBlock_FUNC_LIST_ADDRESS` slot `0/1/2` correspond to `MF_MemoryBlock_Malloc/Reset/Release`; however `hook_vm_mf_memoryblock_func()` had still been stubbing the entire `vmMfMemoryBlockManager` table.
- confirmed code change:
  - `src/main.c` now appends a minimal `kind=0x1E subtype=3` scene-channel object to the built-in fresh-enter `type=1` response, carrying `curpage=1`, `pagenum=1`, `roomnum=0`, `roomlist=""`, `npcnum=0`, and `npcinfo=""`. This is a focused protocol experiment to test whether the current fresh-enter stall is caused by a missing room/NPC-list push after `0x1E/1`, not by another persistent local loading-overlay loop.
  - `src/main.c` now also appends a minimal `kind=0x1E subtype=7` scene-channel object after that `0x1E/3`, carrying `roomid=0`, `rolenum=0`, and `rolesinfo=""`. This follows the next scene-content handler visible in static dispatch and tests whether the fresh-enter stall is waiting for the room-roles push after the room-list push.
  - `src/main.c` now extends the read-only business-handler trace set with `sub_1039430()` (`0x01039430`) so the next rerun can show whether the appended `0x1E/7` actually lands in its consumer.
  - `src/main.c` now upgrades both fresh-enter scene-content blobs from empty shells to one-record test payloads:
    - `0x1E/3 roomlist` now carries one room entry encoded as `u32 roomid=1001` plus three strings (`"1001"`, `"Penglai"`, `"Scene"`)
    - `0x1E/7 rolesinfo` now carries one role record encoded as four strings (`"Codex"`, `"Warrior"`, `"Lv1"`, `"Ready"`) with `roomid=1001`, `rolenum=1`
  - this is the first experiment that lets the `roomlist` / `rolesinfo` parsing loops execute with non-empty content instead of stopping at zero-count fast paths.
  - `src/main.c` now also appends the list-header metadata those same consumers explicitly parse:
    - `0x1E/3` adds `colnum=4` and `colnames=["id","room","scene","state"]`
    - `0x1E/7` adds `colnum=4` and `colnames=["name","job","level","state"]`
  - this is a small protocol experiment driven directly by `scene_parse_column_names()`: it keeps the current one-record rows unchanged and only fills the header fields the client already proved it is prepared to read.
  - `src/main.c` now also adds read-only tracepoints at `scene_parse_column_names()` entry (`0x01039180`), per-column copy (`0x010391E8`), and the return sites of `scene_parse_column_names()`, `parse_scene_npcinfo_list()`, and `sub_1039430()` (`0x010391F8`, `0x01039372`, `0x01039526`). The next rerun should tell us whether the client stalls while decoding `colnames`, or only after list-header parsing succeeds and row parsing begins.
  - `src/main.c` now also stops using the inner-length `vm_net_mock_put_object_blob()` encoding for the fresh-enter scene-list sequence fields `colnames`, `roomlist`, `npcinfo`, and `rolesinfo`. Those fields now use a raw object-entry helper so the scene-list parsers see the sequence bytes directly, matching the latest trace evidence that the old extra `u16 dataLen` wrapper was being misread as the first string length.
  - `src/main.c` now also adds a read-only `trace_scene_tick_gate_change` watcher for `R9+23655/+23656` and extends `trace_loading_overlay_call` to print those two bytes alongside `overlayState`. The next rerun should show which PC first changes the actual `scene_runtime_tick()` loading gate, instead of only the unrelated `loadFlags=...` bytes at `R9+23673..23675`.
  - `src/main.c` now stops feeding the synthetic scene-text path from `motionResource`: both `0x1B/12.name` and the second trailing `actorinfo` string now use a dedicated `vm_net_mock_scene_key_name()` helper, with override `CBE_SCENE_KEY` and default value equal to the existing GBK `c00蓬莱仙岛_01` mock scene key.
  - `src/main.c` now keeps the read-only business-handler tracepoints, but reverts the built-in `type=2/3` replies away from the disproven `kind=0x0A subtype=0x1A scene+posinfo` experiment and back to `kind=7 subtype=20/32` carrying `pcimg` / `expcard`, so the next rerun can confirm whether the client reaches `net_handle_misc_player_fields()` as expected.
  - `src/main.c` now also appends a minimal `kind=0x1E subtype=1` `scene + posinfo` object to the existing `type=1` composite reply as a focused protocol experiment, because static code shows that exact handler is the normal scene-enter path that clears `sceneGate`.
  - `src/main.c` now also treats `maptype/mapID/exitID` requests as composite WT packets rather than map-only packets: when the request also carries `0x1B/11` and/or `0x0C/1 + 7/42`, the built-in response appends a minimal empty `0x1B/11` object and the existing learned-skill/books objects alongside the `0x1E/2 scene+posinfo` reply.
  - `src/main.c` now also adds read-only tracepoints for `net_handle_business_followup_events()` entry (`0x01010594`) and the three most relevant follow-up branches currently in scope: `12/1` (`learnednum`/`learnedskill`), `27/11`, and `27/4`. The next rerun should answer whether the composite post-enter reply is failing because those follow-up objects are never scanned, or because they are scanned but still semantically incomplete.
  - `src/main.c` now also adds read-only tracepoints for `scene_send_type27_followup_request()` (`0x0100ED20`), the primary `net_handle_fb_target_dispatch()` (`0x01010BC0`), and `parse_scene_npcinfo_list()` (`0x01039222`) so the next rerun can show whether the client emits the still-unseen `27/4` request or later consumes a scene-channel `0x1E/3` NPC/room list packet.
  - `src/main.c` now also extends the composite scene-change reply experiment one step further: when the request carries `0x1B/11`, the response still keeps the empty `0x1B/11` object that the follow-up scanner already consumes, but now also appends a minimal `0x1B/12` object with `result=1`, `fb=1`, `name="Codex"`, and `posinfo=(120,120)` so the primary `net_handle_fb_target_dispatch()` path can apply named-target state before the later `27/11` follow-up case runs.
  - `src/main.c` now also adds a dedicated built-in response for the newly exposed `len=34` type-27 follow-up composite request. When the request carries `0x1B/4`, the emulator now returns a combined packet with `0x19/12 result=4` (if requested), `0x1B/11`, `0x1B/4(min=0,result=1,type=request.type,fb=1,info=\"\")`, and `0x07/42(booknum=0,booksinfo=\"\")`.
  - `src/vmFunc.c` now treats no-extension update cache files such as `MMORPGTempbin` / `mmorpg_updatetempbin`, plus `.bin` files generally, as binary-open candidates in `vm_file_ext_requires_binary()`, so host `fopen()` no longer uses Windows text mode for downloaded update payloads.
  - `src/main.c` now changes the minimal synthetic `0x1B/12` object so its `name` field is `MMORPGTempcbm` instead of the placeholder player name `Codex`, because earlier runs demonstrated that the later resource-miss path was opening/downloading the same string; this is now understood as a temporary protocol workaround rather than the correct long-term semantic.
  - `src/main.c` and `src/vmFunc.c` now also write a one-line `session_start` marker into `logs/net_trace.log` and `logs/storage_trace.log` the first time each log is touched in a process, so appended runs are easier to segment during reverse-engineering.
  - trace output paths now go to `logs/net_trace.log` and `logs/storage_trace.log` relative to the runtime working directory, and the emulator creates the `logs/` directory lazily before first write.
  - `src/main.c` now also has a read-only `trace_loading_flags_change` watcher that logs the first PC/LR whenever `Global_R9+0x5C79..0x5C7B` or `loadMode` changes, together with the current scene object and scene-local loading bytes. This is meant to catch the exact writer for the newly narrowed `tick=113 -> tick=116` `0,0,0 -> 1,0,0` transition.
  - `src/main.c` now also includes read-only tracepoints for `scene_send_map_enter_request()` (`0x0100F9B4`) and the candidate business handlers `net_handle_misc_player_fields()` (`0x01011C88`), `net_handle_role_login_gift_glamour()` (`0x010114FC`), `net_handle_scene_posinfo_response()` (`0x01010950`), and `net_handle_scene_channel_dispatch()` (`0x01039B8A`) so the next run can show whether loading ever reaches a real scene-enter request/response path.
  - `src/runtime/emulator_runtime.c` now routes `vmMfMemoryBlockManager idx=0/1/2` to the existing `vm_MF_MemoryBlock_Malloc()`, `vm_MF_MemoryBlock_Reset()`, and `vm_MF_MemoryBlock_Release()` implementations, with trace logs added for each call.
- existing `bin/net_trace.log` shows the login-success path does advance past `mmTitleMstarWqvga.cbm` into the main CBE loading/scene path: after the login actor response, the title screen removes itself, the runtime resumes the lower screen, and a new dynamic scene/loading screen is pushed (`01053f78`) with the correct pool-module `R9` saved earlier.
- existing `bin/net_trace.log` also shows the first post-login follow-up requests are not absent: the client sends three `type=1/2/3` game packets on connect `2`, and the current mock returns built-in responses of lengths `129/34/36` before the crash.
- in an older modified-runtime trace, the path reached `scene_runtime_tick` (`0x0101517A`) and `scene_draw_status_panels` (`0x0101466A`) before failing with a client-side `SIGABRT` through `sub_104D538 -> sub_104E7C8 -> sub_104EA2C`.
- refined hypothesis:
  - the current loading stall is protocol-semantic rather than crash-related. A now-confirmed sub-hypothesis is that `type=2/3` must not be forced onto `0x0A/0x1A`, because that only feeds the name/money/gift/glamour handler. The next narrow question is whether the older `pcimg/expcard` path is sufficient by itself, or whether another post-login response is still missing before the client emits a real scene-enter / scene-channel request (`0x1C` or `0x1E`) or calls `scene_send_map_enter_request()`.
  - the strongest current protocol hypothesis is now that the missing semantic is an initial scene-enter packet, not another `pcimg/expcard` field: the client has already consumed the `0x0A/0x1A` name/money packet and the `7/20` + `7/32` misc-field packets, yet remains blocked behind `sceneGate=1` without ever entering `kind=0x1E subtype=1`.
  - after that hypothesis was confirmed and fixed, the next strongest protocol hypothesis is that the remaining loading stall comes from the rest of the post-enter composite request, not from the scene-enter packet itself. The clearest candidates are the embedded `0x1B/11` follow-up event and the repeated `0x0C/1 + 7/42` learned-skill/books request pair that the emulator had not been answering inside the same `len=86` packet.
  - the strongest current sub-hypothesis is now inside `net_handle_business_followup_events()` (`0x01010594`): either the client never scans the appended `12/1` and `27/11` objects from the composite scene-change reply, or one of those branches is the missing contract that finally clears the loading overlay/state after the scene screen already starts drawing.
  - that earlier sub-hypothesis is now partly disproven: the client does scan and consume both `12/1` and `27/11`, but loading still remains. The next strongest candidates are therefore a later `27/4` follow-up exchange or a scene-channel `0x1E/3` room/NPC list packet (`curpage`, `pagenum`, `roomnum`, `roomlist`, and likely downstream `npcnum` / `npcinfo` semantics) that has not yet appeared in the trace.
  - a more focused protocol sub-hypothesis now sits in the `0x1B` chain itself: request object `0x1B/11` may expect a server reply shaped as `0x1B/12` rather than a bare echoed `0x1B/11`. Static code shows subtype `12` is the first place that actually writes `fb`, `name`, and `posinfo` into live UI/scene target state, and the later `27/11` follow-up logic explicitly branches on that stored `fb` byte.
  - because the triggering `len=86` request contains no resource-like `name` field and because `0x1B/12.name` aliases the same buffer later reused as outgoing `mapID`, the strongest current semantic guess is “named target / scene label / current location text derived from map-enter context”, not “motion resource” and not “bundle filename”.
  - the strongest current runtime hypothesis has shifted again: now that `0x1E/3` and `0x1E/7` both parse correctly, `R9+0x5C64+3/+4` already reach `1,1`, and no newer scene-overlay traces appear after the first fresh-enter `event=7`, the original post-login loading blocker is likely no longer in the loading path itself. The next real gap is more likely a later in-scene contract, UI mode, actor/content initialization, or another business-side state transition that happens after the first fresh-enter callback.
  - within that narrowed space, the strongest sub-guess is now “internal current scene id / scene key” rather than a localized display label, because the fresh-enter and restore validators explicitly accept `'c'`-prefixed strings and the observed `mapID` on the wire is `c00PenglaiXiandao_01`.
  - the producer question is now partly resolved: `R9+0x5E46` is not first generated inside `scene_runtime_init_and_sync()`. `parse_actorinfo_response()` (`0x0100FA88`) copies a late trailing `actorinfo` string field directly into `R9+0x5E46` at `0x0100FD00 .. 0x0100FD08` and then returns that buffer pointer. This means fresh-enter is reusing a value already parsed from login `actorinfo`.
  - that new parser evidence creates a concrete semantic mismatch in the current mock: `src/main.c:vm_net_mock_build_actor_info()` still fills the second trailing `actorinfo` string from `motionResource`, but the consumer side now looks much closer to “scene/map key or scene-resource identifier” than to “motion resource filename”.
  - local tracked assets strengthen that scene-key reading: `bin/JHOnlineData/` already contains scene files `c00蓬莱仙岛_01.sce` and `c00蓬莱仙岛_03.sce`, while the currently observed outgoing mock key is the romanized placeholder `c00PenglaiXiandao_01`.
  - the newest clean rerun confirms the scene-key split is now feeding the local scene-resource family rather than actor resources. `logs/storage_trace.log` shows `c00蓬莱仙岛_01` resolving into `c00蓬莱仙岛_01.sce`, then `00蓬莱仙岛_01.map`, then map art such as `m_peak1.gif`, `m_village05.gif`, and `m_apotheosis.gif`.
  - the same rerun narrows the remaining loading stall to a local runtime-state transition rather than the earlier scene-key/resource-family mismatch. `logs/net_trace.log` shows `loadFlags=0,0,0` at `tick=113` after the pre-scene callback (`activeScreen=010534b4`), but by `tick=116` the scene screen is already active (`activeScreen=01053450`) and `loadFlags` are already `1,0,0` before the first post-enter business event is dispatched. After `kind=30 subtype=1` runs, `sceneGate` clears to `0` and `sceneState` becomes `7`, but `loadFlags` stay `1,0,0`.
  - the same clean rerun also disproves one recent local-loading guess: none of the read-only tracepoints at `sub_101A97C`, `sub_101AED2`, `sub_101D2D6`, `sub_101D4B4`, or `sub_101EC62` fired in that session, so the current `loadFlags=1,0,0` transition is not yet going through those expected helper paths.
  - the newer rerun with `trace_loading_flags_change` now pins the first actual writer: `0x0101029A` inside `sub_1010228()` sets `R9+0x5C74` and `R9+0x5C79` to `1`, and the watcher fires at function return `pc=0101029c` during `tick=125`. The transition happens before the first `kind=30 subtype=1` scene-enter business packet is dispatched.
  - `sub_1010228()` currently has only two confirmed callers in IDA: `scene_runtime_init_and_sync()` (`0x01013594`) and `net_handle_group_info()` (`0x010124AA`). In the live rerun, the flag transition happens before any observed `groupinfo` handler activity, so the strongest current reading is that scene initialization itself is calling `sub_1010228()` and turning on this state.
  - static decompilation of `sub_1010228()` makes the semantic of `0x5C79` less certain than our earlier `loadFlags` label suggested. The function zeroes a `0x130`-byte struct at `R9+0x5CF4`, copies several fields and a string from the object at `R9+0x5CA4`, sets `dst[0]=1`, clears `R9+0x5CF8`, and then sets `R9+0x5C74=1` and `R9+0x5C79=1`. Because the same helper is reused by `net_handle_group_info()`, this byte may belong to some scene/group side-state rather than being the sole root cause of the `"正在加载中..."` overlay.
  - the newest rerun did not emit the intended `trace_sub_1010228_entry` line even though `trace_loading_flags_change` still shows execution reaching `0x0101029A/0x0101029C`. That means the entry hook itself is not yet a reliable source of truth for this function, likely due to instruction-address granularity or entry-address mismatch in the emulator hook, not because `sub_1010228()` failed to execute.
  - to avoid over-interpreting that missing entry trace, the next narrower instrumentation now moves to the only two static callsites of `sub_1010228()`: `scene_runtime_init_and_sync()` at `0x01013594` and `net_handle_group_info()` at `0x010124AA`. Those hooks log the same copied source string/object fields immediately before the call.
  - the latest rerun confirms the live caller: `trace_sub_1010228_callsite label=scene_runtime_init_and_sync` fires immediately before both `trace_sub_1010228_entry` lines and the later `trace_loading_flags_change pc=0101029c`. There is no matching `net_handle_group_info` call in the same session.
  - the same trace also weakens the older assumption that `R9+0x5C79` is itself the root loading-overlay latch. `sub_1010228()` is being called from scene initialization with `flagsBefore=0,0`, a non-text `srcObj` (`05400000`), and a non-ASCII `copiedName` pointer (`05400044`), which looks more like internal scene/group state propagation than a direct UI text/overlay gate.
  - the new `lcd_text_call` trace confirms the current `"正在加载中..."` evidence is still one layer too shallow: all repeated draws come through the `vMDrawString` manager wrapper (`lr=0100488d`, `last=0100488a`), so the trace is seeing the generic guest LCD API shim rather than the higher-level screen function that requested the text.
  - the wrapper's first few stack words vary (`st[0]=fefefefe`, `050195e4`, `01055970`, `0`, `8`, `6` across repeated draws), which suggests the real caller context is preserved in the wrapper frame rather than in current `LR`. Because `sub_1004836` pushes `{R0-R7,LR}` before entering the host manager callback, the next reliable source of truth is the saved guest LR at `SP+0xEC`.
  - the saved-LR trace now resolves the `"正在加载中..."` caller: repeated loading-text draws consistently show `guestCaller=01035205`, which is the `BLX` inside `sub_10351BA`, the centered single-line text helper. The saved `R0` at wrapper entry is `0x01003648`, and that ROM string decodes to `正在加载中...`.
  - static xrefs then close the source chain: `0x01003648` is only referenced inside `sub_100337C`, where case `n2==0` calls `sub_10351BA(&unk_1003648, 120, 160, 0xFFFFFF)`. `scene_runtime_init_and_sync()` calls `sub_1003568()` immediately after `sub_1010228()`, and `sub_1003568()` is just `return sub_100337C(0);`. So the loading overlay now has a confirmed local source in the scene-init path itself, not only in the startup/update screen.
  - this separates two different UI paths cleanly:
    - `sub_100337C(0)` provides the local centered `正在加载中...` overlay used by scene/runtime initialization
    - `startup_screen_update_draw()` / `sub_103A84A()` / `sub_103A8E4()` are a different startup/update-screen family, including multiline communication text and settings/update messages
  - that sub-hypothesis is now partially confirmed: adding `0x1B/12` does not immediately clear loading, but it does unlock the next real client request, which includes `0x1B/4(type=1)`. Static code for the later `27/4` follow-up path reads fields `min`, `result`, `type`, `fb`, and `info`, so the next narrow question is whether a minimal successful `27/4` response is sufficient to move `loadFlags` or trigger still-later requests.
  - the strongest current runtime hypothesis is no longer “scene packets are still missing” but “the resource-update branch triggered by the new `27/4` path cannot complete because host file-open semantics are wrong for binary update temp files.” That hypothesis is now supported by both static code and mismatched file sizes in `logs/storage_trace.log`.
  - that runtime hypothesis is now confirmed fixed, and the later `Codex` reopen loop is also confirmed fixed in the newest run: changing the synthetic `0x1B/12 name` to `MMORPGTempcbm` prevents the later `JHOnlineData/Codex` open failure and allows the update/loading callback to clear `loadFlags` and remove the update screen. The deeper protocol problem now looks like a semantic mismatch: `0x1B/12.name` should describe a named target/location, while the later `type=6.name` is a resource filename, so some not-yet-identified intermediate path is reusing the former as the latter.
  - the post-login loading path depends on the DF_DataPackage manager contract much earlier than the old modified-runtime trace suggested; more missing manager entries may need to be reconnected to the existing `src/vmFunc.c` implementations before the client can reach scene/status drawing on an unmodified runtime.
  - the current `tick=66` abort is most likely caused by incomplete `vmMfMemoryBlockManager` semantics during event-packet allocation or reset, because the guest reaches `sub_1034518 -> sub_103496C/0x103498C` right before the crash and those helpers dispatch through the memory-block function table that had been stubbed.
  - after removing the progress-forcing hook, the true unmodified blocker may reappear earlier than `scene_draw_status_panels`; this needs a fresh reproduction.
  - if the path still reaches `scene_draw_status_panels`, the most likely immediate cause remains a divide-by-zero or similarly uninitialized status-panel field during one of the two ratio calculations at `0x0101477E` or `0x010147AE`.
  - newest live rerun on the corrected `1/1/12` path confirms that this divide-by-zero branch is active again. In that session, title-side runtime on `activeScreen=010534b4` locally loads `tile25/UI1.gif`, `tile26/UI2.gif`, and `tile29/UI6.gif` before scene entry, so the old null-image `tile26` crash is no longer the first blocker there; stdout now ends at `Arithmetic exception: Divide By Zero` with `pc=0x0104E980`, `lr=0x01014785`, which lines up with the first segmented-bar ratio in `scene_draw_status_panels()`.
  - the new diagnostic question is therefore narrower than before:
    - which primary/secondary status-bar denominator is zero on the corrected `1/1/12` path
    - whether the zero comes from `sceneCurrentNode` base-max fields (`+0xBC` / `+0xC0`) or from `sceneStatusMeterNode` display-max fields (`+0xC4` / `+0xC8`)
  - `src/main.c` now logs `trace_status_bar_divide_site` at both divide sites (`0x0101477E` and `0x010147AE`) so the next reproduction should answer that directly.
  - that reproduction is now in, and it rules out the source-node-max hypothesis. The first crashing divide site is `trace_status_bar_divide_site label=primary pc=0101477e`, with `current=0`, `baseMax=120`, and `displayMax=0`, so the live denominator is `sceneStatusMeterNode + 0xC4`, not `sceneCurrentNode + 0xBC`.
  - the follow-up low-noise state trace makes the zero more specific: during the visible first-frame init sequence, `scene_copy_status_snapshot_node()` repairs the primary/secondary base maxima to `120/100` (`0x01045024..0x0104503C`), but `primaryDisplayMax` never becomes non-zero before `scene_rebuild_status_meter_node()` copies meter values back into `sceneCurrentNode` at `0x0101021A`.
  - static caller audit now suggests a concrete init-contract mismatch. `scene_runtime_init_and_sync()` calls `scene_rebuild_status_meter_node(1)` at `0x0101363E`, but the only built-in fallback that seeds `sceneStatusMeterNode + 0xC4/+0xC8` from current-node base maxima is guarded by `!*(R9+0x6048) && n2==2` inside `scene_rebuild_status_meter_node()` (`0x0100FF26..0x0100FF3E`). The current fresh-enter path therefore skips that fallback and can write zero accumulators back into the meter node at `0x010100D2..0x010100E0`.
  - `n2==2` is also confirmed live elsewhere, so this is not dead code. `sub_1033544()` has a path that, when `*(R9+0x6048)==0`, calls `scene_rebuild_status_meter_node(2)` at `0x010335EA` and then immediately `scene_rebuild_status_meter_node(1)` at `0x010335F0`. By contrast, the current first-frame callers `scene_runtime_init_and_sync()`, `sub_1011ACA()`, `sub_101CD78()`, and `scene_apply_levelup_status_growth()` all use only `n2==1`.
  - the first live rerun with the new rebuild-site watcher now confirms the seed-gate half of that theory directly. At `0x0100FF26`, the crashing first-scene path logs `n2=1` and `sourceHead=00000000`, while current/meter both already read `baseMax=120/100` and `displayMax=0/0`. In other words, the client reaches the exact gate where the `n2==2` fallback would have helped, but the live first-scene path is still taking the non-fallback mode.
  - the next rerun shows the situation is even starker than a “bad writeback value” hypothesis. There is still no `writeback_early` (`0x010100D2`) or `writeback_common` (`0x010101E8`) trace at all before the function reaches `0x0101021A`. Static control flow around `0x010100EC` explains this: with `sourceHead==0`, the current path reaches `CMP R5,#1 / BNE 0x010101D4`, skips both explicit display-max writeback blocks, and then falls through to the final clamp/copy tail that simply preserves the meter node's existing `0/0` display maxima.
  - the source head now has a concrete owner and bucket meaning. `scene_system_bootstrap()` initializes a structured manager at `R9+0x6040` via `sub_10332BE(R9+0x6040, 64, 74)`, and that manager's bucket router `sub_1031CF4()` maps runtime record `type=15` to offset `+8`. So `R9+0x6048` is specifically the head of the manager's `type=15` list.
  - the first confirmed producer for that exact bucket is also now known. Manager helper `sub_1032B8A()` (installed at manager `+0x68`) clones a record, force-sets the cloned record's type byte at `+282` to `15`, and inserts it through the common add path. `net_handle_misc_player_fields() -> sub_1033544()` reaches that helper on at least two packet-success branches: top-level business subcommand `7`, then packet-local subtype `8` with parsed `type=4, result=1`, and packet-local subtype `9` with `result!=0`. Those branches then rebuild the HUD meter immediately, and the `subtype=8/type=4` path is also the already-confirmed place that does `if [R9+0x6048]==0 -> rebuild(2) -> rebuild(1)`.
  - current mock traffic now sharpens that protocol suspicion further. The post-login `len=35` request already bundles a top-level `1/7/7 type=1` object alongside `1/5/10 id=...`. The previous built-in `vm_net_mock_build_group_type1_response()` only used that `type=1` bit to append `0x0A/0x1A` plus `0x1E/1/3/7`, with no top-level `1/7/*` reply at all. A focused `1/7/8 {type=4, result=1, seq=1}` experiment was then tried because static `sub_1033544()` shows that exact branch can seed the empty `type=15` HUD source head and immediately run the safe `rebuild(2) -> rebuild(1)` sequence.
  - that runtime experiment is now a confirmed negative result for the default login path. The packet really reaches `trace_business_handler ... kind=7 subtype=8`, but it crashes earlier inside `sub_1033544()` at `sub_104D424()`, because the branch first clones `*(R9+38020)` and that local source pointer is still null on the current post-login path. So `subtype=8/type=4` is not a safe generic answer to bundled `1/7/7 type=1`; it depends on another earlier local setup step.
- missing evidence:
  - which earlier request/response stage is supposed to populate the local pending object pointers `*(R9+38020)` and `*(R9+38024)` before any `top-level 7 -> subtype 8/9` success reply is processed
  - whether the real bundled `1/7/7 type=1` answer is a different top-level `7` subtype entirely, rather than the now-disproven generic `subtype=8/type=4` shortcut
  - whether there is any second non-packet producer for the same `type=15` bucket, or whether `sub_1033544()` is effectively the only source that matters for the first safe HUD-meter rebuild
  - whether the correct first-frame contract is still “seed that `type=15` source before `rebuild(1)`”, or instead “call `rebuild(2)` once before the first `rebuild(1)` when the `type=15` head is still empty”
- next experiment:
  - stop prioritizing more writeback tracing. The next narrow protocol task is now to identify the producer/setup stage for `*(R9+38020)` / `*(R9+38024)` before retrying any `7/8` or `7/9` success reply. Until that stage is understood, keep the `1/7/8` experiment gated behind `CBE_GROUP_TYPE1_MISC_SYNC8=1` rather than using it on the default path.
  - if protocol-side evidence still comes up empty, do one last static sweep for other indirect callers of manager `+0x68` / `sub_1032B8A()` so we can rule out a second local producer before concluding that the missing `type=15` head is a business-packet gap rather than a local init callback.
  - rerun with the new `type=1 + scene-channel 0x1E/1` experiment and capture whether the loading path now logs `trace_business_handler pc=01039b8a kind=30 subtype=1`, whether `sceneGate` clears to `0`, and whether any later `scene_send_map_enter_request()` or scene runtime path appears.
  - rerun with the new composite scene-change response and capture whether the added `0x1B/11` and `0x0C/1 + 7/42` responses clear the remaining loading state, or whether the next missing semantic is further down the scene-channel sequence such as `0x1E/3` (`npcnum/npcinfo`) or another post-enter request object.
  - rerun with the new follow-up tracepoints and capture whether `trace_followup_scan_enter` appears after the `0x1E/2` response, and whether any of `trace_followup_case label=case12_1`, `case27_11`, or `case27_4` are hit. If none of them fire, the remaining gap is likely object ordering/count/packet-shape; if they do fire, inspect which branch leaves `loadFlags=1,0,0` unchanged.
  - rerun with the new scene/type27 tracepoints and capture whether `trace_scene_send_type27_followup_request` appears, whether any primary `trace_business_handler pc=01010bc0 kind=27 subtype=4` arrives later, and whether any `trace_business_handler pc=01039222 kind=30 subtype=3` scene-channel NPC/room list path appears. Those traces should separate a missing `27/4` exchange from a missing `0x1E/3` scene payload.
  - rerun with the new `0x1B/12` experiment and capture whether the composite scene-change reply now logs `trace_business_handler pc=01010bc0 kind=27 subtype=12`, whether `trace_scene_send_type27_followup_request` appears afterwards, and whether `loadFlags` finally change. If not, the strongest remaining candidate is still a later `0x1E/3` NPC/room list or another unseen post-enter request.
  - rerun with the new built-in `27/4` combo response and capture whether the log now shows `mock_type27_followup_combo_response`, whether `trace_followup_case label=case27_4` finally appears, and whether `loadFlags` or `sceneState` move again after the `len=34` request. If that still stalls, revisit `0x1E/3` room/NPC list as the next candidate.
  - rerun with the rebuilt binary after the binary-open fix in `src/vmFunc.c`, then confirm whether `logs/storage_trace.log` now shows `MMORPGTempbin` sizes matching raw bytes (`20480 -> 40960 -> 48858`), whether a later `file_close` / `file_rename` appears for the update temp file, and whether the loading/update screen finally exits on its own.
  - use the new `session_start` markers to isolate one clean run, then trace the first issue after the now-confirmed loading teardown. The best next evidence is the first code path that takes the `0x1B/12`-derived display name and passes it into a local resource loader (`sub_100D2BE` / `sub_1044DA0`) before `send_update_chunk_request()` serializes it as `type=6.name`.
  - rerun with the new read-only resource-name tracepoints at `sub_100D2BE()` (`0x0100D2BE`), `sub_1043206()` (`0x01043206`), `sub_1044DA0()` (`0x01044DA0`), `sub_1044EF6()` (`0x01044EF6`), and `send_update_chunk_request()` (`0x01036C66`). Compare each resource `name` against the active UI cached `0x1B/12` display name and the later `type=6` pending-entry name to determine which caller first reuses the display string as a resource key.
  - now that `parse_actor_motion_descriptor()` is confirmed as the immediate caller that forwards the `0x1B/12`-derived UI name into `load_resource_stream_by_name()`, the next experiment is to inspect its function-entry caller `LR` and incoming arguments on the live scene path. The narrow question is which upstream caller and which entry argument (`arg0` vs the stacked extras such as `arg4`) first carries `MMORPGTempcbm`, and whether that value comes from a scene-node label/current-actor buffer rather than a real motion-resource field.
  - stop treating existing local `.actor` files as likely candidates for this field. The next useful experiment is to recover a real motion-descriptor sample or its true naming rule from static code / captures / update payloads, or to prove that `0x1B/12.name` itself should not feed this path in the first place. A focused next trace target is the producer of `currentMapIdText` / UI-name data that `scene_rebuild_runtime_nodes()` forwards into `sub_10352AE()`.
  - stop treating the second trailing `actorinfo` string as a motion-resource field in the mock. The next narrow experiment should instead test a dedicated scene-key value there, preferably with a separate helper/env var from `motionResource`, and compare whether the client expects a full `.sce` filename (`c00蓬莱仙岛_01.sce`) or an internal extensionless key derived from the same scene resource family.
  - that helper split is now implemented. The next rerun should capture whether the client still misroutes the scene-text slot into actor/image parsing, and if so, whether the first failing/local-open name now looks like a scene resource family (`c00...`) rather than a player actor resource.
  - rerun with the freshly rebuilt binary that now also implements `vmMfMemoryBlockManager idx=0/1/2`, then capture whether event `5` proceeds into a version-request send path or exposes the next missing manager/interface.
  - rerun with the new `trace_loading_flags_change` watcher and isolate the very first PC/LR that changes `loadFlags` from `0,0,0` to `1,0,0`. The current log already narrows that window to after `tick=113` post-callback (`activeScreen=010534b4`) and before `tick=116` pre-business dispatch (`activeScreen=01053450`), so the next evidence should decide whether the remaining blocker is in scene-screen construction, local `.sce/.map` load completion, or some still-untraced loading-state helper.
  - rerun with the new `trace_scene_runtime_tick` hooks at `scene_runtime_tick` entry / draw-pass / status-panels / actor-pass (`0x01014D3A`, `0x01015158`, `0x0101517A`, `0x0101517E`). The narrow question is whether the fresh-enter path reaches the normal world/update pass after `tick=138`, and if so which early scene-object mode bytes (`sceneObj+1/+2`) and `sceneTickGate` / `loadFlags` values it holds there.
  - rerun with the new `trace_draw_map_tile_entry` hook at `draw_map_tile_by_index()` entry (`0x0100E8B0`) and the new `logs/stdout_trace.log` mirror. The key missing evidence is which `tileId`, `tileIndex`, and resolved image pointer are null on the failing path that currently dies at `0x0100E67C`, and whether the bad caller is `scene_draw_status_panels()` (`tiles 26/65`) or another scene draw site.
  - the latest per-tile loader trace answers most of that already: `tile26` is currently in the "never requested at all" bucket, while `tile65` is requested as `UIsh7.gif` and succeeds locally. Static analysis now pushes this one level further: around `scene_runtime_init_and_sync() -> sub_1045EA4()` itself there is no second confirmed `25/26` preload caller, and the post-preload deferred-popup branches are not piclib loaders.
  - the best next target is therefore no longer “another hidden branch right next to `sub_1045EA4()`”, and also no longer the earlier broad “whole `UI1..UI8` family is missing” guess. The narrower question has split in two:
    - which title-module transition-screen path around `sub_C82() / sub_A50()` makes `UI7/UI8` visible early, and whether it ever hands off anything into the later scene resource/cache layer
    - which separate later scene-local path was supposed to request `tile26/UI2.gif` as a scene-piclib slot before `scene_draw_status_panels()` became visible
  - the latest widened `trace_status_tile_slots` and `trace_piclib_slot_load` run already answered the earlier runtime questions: by the first crash, the missing set is specifically `25/26/29`, while `27/28/30/31/65` are loaded; `tile30/31` are not scene-preloader products, but come from an earlier `activeScreen=010534b4` path. The new title/startup static passes refine that again: the early phase(s) request `UI2/UI7/UI8` by filename through the shared lower-level loader, but there is still no evidence that either path populates scene piclib slot `26`.
  - a side result from the same session is now confirmed too: the scene-list wrapper fix is good enough for this branch of the login flow. `scene_parse_column_names()` prints sane short names for both `0x1E/3` and `0x1E/7`, and both `scene_roomlist_return` and `scene_roles_return` fire before the later status-panel crash. So the active blocker has cleanly moved back to the local render/tile contract, not malformed `colnames` blobs.
  - rerun with the new `trace_sub_1010228_callsite` hooks and capture whether `label=scene_runtime_init_and_sync` or `label=net_handle_group_info` fires in the same session as the `0,0,0 -> 1,0,0` transition. That should give a direct caller answer without depending on the missing function-entry hook.
  - now that the `sub_1010228()` caller is confirmed, stop over-indexing on `0x5C79` as the only likely root cause. The next narrow experiment should trace the actual guest-side `vMDrawString` / `vMDrawStringEx` caller that paints `"正在加载中..."`, so we can distinguish a persistent loading-screen draw path from the auxiliary scene-init state that merely happens to flip at the same time.
  - rerun once more with the augmented `lcd_text_call` trace that now logs the wrapper's saved registers and `guestCaller` (saved LR from `SP+0xEC`). That should identify the real guest function that keeps painting `"正在加载中..."`.
  - now that the string source and local call chain are confirmed, the next narrow question is temporal rather than structural: which callsite keeps re-entering `sub_1003568()/sub_100337C(0)` after the first scene-enter event, and what local state byte around `R9+19638` or scene-init follow-up path is supposed to stop it. A minimal next trace on `sub_1003568`, `sub_100337C`, and `sub_1003CFC` should answer whether the overlay persists because scene-init never clears once, or because another loop/timer keeps redrawing it.
  - rerun with the freshly rebuilt binary and check whether the fresh-enter `type=1` reply now logs `mock_group_type1_response ... sceneRoomNpc=1`, whether any `trace_business_handler pc=01039222 kind=30 subtype=3` finally appears, and whether that changes the current `sceneGate=0 sceneState=7 loadFlags=1,0,0` ending state.
  - that follow-up rerun is now complete: `0x1E/7` also lands and still leaves `loadFlags=1,0,0`. The next narrow experiment should stop adding more empty scene-channel shells and instead give `roomlist` and/or `rolesinfo` the minimum non-empty structure that their consumers expect.
  - that non-empty follow-up is now implemented. The next rerun should check whether `loadFlags` finally move or whether a new downstream request/state change appears once `parse_scene_npcinfo_list()` and `sub_1039430()` both iterate at least one record.
  - that row-content experiment is now extended with `colnum/colnames`, because both live consumers parse those fields before row iteration. The next rerun should first answer whether list-header metadata alone is enough to unblock scene readiness before we spend another round guessing extra room/role fields.
  - that first `colnum/colnames` retry now appears malformed: the next rerun should look specifically for `trace_scene_colnames_entry`, `trace_scene_colnames_item`, and `trace_scene_list_return` so we can pinpoint whether the stall happens in list-header parsing or after it returns.
  - that malformed retry is now narrowed to an extra wrapper-layer issue. The next rerun should show whether removing the inner `dataLen` wrapper lets `trace_scene_colnames_item` produce sane short column names (`id`, `room`, `scene`, `state`) and finally allows `parse_scene_npcinfo_list()` to return.
  - static consumers suggest the best next target is payload shape rather than subtype coverage:
    - `parse_scene_npcinfo_list()` loops over `roomnum` entries and reads four string columns per room from `roomlist`
    - `sub_1039430()` loops over `rolenum` entries and reads repeated string columns from `rolesinfo`
  - because both current mock payloads use zero counts plus empty blobs, the strongest next hypothesis is that the scene screen waits for at least one valid room/role record, or for some downstream side effect that only occurs once those loops execute with real content.
  - rerun with the freshly rebuilt binary that now implements `vmDfDataPackageManager idx=0` and `idx=4`, then capture the next missing DF_DataPackage entry or the first non-DataPackage failure after those two calls are satisfied.
  - reproduce after removing the tile backfill / missing-tile skip hook and capture the first failing PC/LR plus surrounding screen/network logs
  - only if the unmodified path still reaches the status-panel divide sites, use the new minimal divisor trace in `src/runtime/emulator_runtime.c` to map the zero denominator back to the owning scene/status structure
  - if the failure comes from network-fed state, extend the mock with the minimum confirmed field needed rather than forcing the scene forward

Suggested entry format for additional questions:

```text
## Question
- status: open
- area: firmware | protocol | runtime | server | assets
- current belief:
- missing evidence:
- next experiment:
```
